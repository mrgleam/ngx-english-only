import { NgxEnglishOnlyDirective } from './ngx-english-only.directive';

describe('NgxEnglishOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxEnglishOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
